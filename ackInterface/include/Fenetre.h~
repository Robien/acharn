#ifndef DEF_FENETRE
#define DEF_FENETRE

#include <irrlicht.h>
#include "InterfaceInfo.h"
#include "Skin.h"
#include "BarreActive.h"
#include "../../ackUtils/include/Referenced.h"

using namespace std;

class Fenetre : public Referenced {
public:
    Fenetre(InterfaceInfo* m_interface,
            irr::core::position2d<irr::s32> m_coord,
            const wchar_t* m_titre,
            int m_hfenetre,
            int m_lfenetre);

    int getId();
    void afficher();
    void cacher();

    //   Fenetre* getFenetre();
    //   int getID();
    //   void setID(int id);

protected:
	irr::gui::IGUIWindow* m_fenetre;
	irr::gui::IGUIEnvironment* m_gui;
    irr::video::IVideoDriver* m_pilote;
	irr::core::rect<irr::s32> cadre;
};	
#endif

